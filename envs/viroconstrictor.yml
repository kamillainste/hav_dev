name: viroconstrictor
# Nanopore amplicon assembly for the WGS pipeline. Invoked as
# `conda run -n viroconstrictor` by scripts/run_pipeline.sh.
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - viroconstrictor
